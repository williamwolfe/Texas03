<%= form_for(@student_evaluation_case2) do |f| %>
  <% if @student_evaluation_case2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_evaluation_case2.errors.count, "error") %> prohibited this student_evaluation_case2 from being saved:</h2>

      <ul>
      <% @student_evaluation_case2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">    
    <%= f.hidden_field :user_id, value: current_user.id %>
   <!-- <%='hidden field = user_id: '%> -->
    <!-- <%=current_user.id%> -->
</div>
  
  
  
  
<h4> 1. How should Fred rate the interpersonal, professionalism, responsibility and communication skills in the PT MACS?</h4>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_area :rate, cols:"60", rows:"4" %>
  </div>
  
  
<h4> 2. Based on your ratings, what should Fred's plan of action be to assist Mark in attaining entry-level ratings on the above skills?</h4> 
  <div class="field">
    <%= f.label :plan %><br>
    <%= f.text_area :plan, cols:"60", rows:"4"  %>
    
  </div>
  <div class="actions">
    <%= f.submit "submit" %>
  </div>
  <div>
  	<small>Note: After you click "submit" your answers will be saved.  You can change/edit your answers at a later date by returning to this page and editing your answers.</small>
  </div>
<% end %>
